import App from './App';

// test('renders learn react link', () => {
//   // test function takes two arguments 
//   // first argument is a description of the test, up to you
//   // second argument is an anonymous function which contains the actual test and code
//   render(<App />);
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// // we check if the element /learn react/ is in the document
// // if it exists in the document, the test will succeed
// // otherwise the test will fail 
// // to test, type npm test in the terminal
// // every time changes are saved, the test reruns automatically
// });


